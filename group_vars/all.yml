---
# Variables applied to every host in the inventory.
# Override per-group in group_vars/<group>.yml or per-host in host_vars/<host>.yml.

# Set the remote user if not already pinned in the inventory
ansible_user: ubuntu

# Common apt cache settings
apt_update_cache: yes
apt_cache_valid_time: 3600
